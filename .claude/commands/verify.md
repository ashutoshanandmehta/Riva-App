---
description: Run the verifier on the current change set
---
Run the `verifier` subagent against `git diff`. Report every finding by category and the
confidence score. Do not fix anything in this command — report only.
